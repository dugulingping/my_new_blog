# ==========================================
# 博客站点基础配置环境变量样例
# 请复制此文件并重命名为 .env，然后按需修改对应的值
# ==========================================

# 博客的正式域名 (用于生成 Sitemap 等绝对链接，必须带 http:// 或 https://)
# 建议在上线部署时配置为自己真实的域名
SITE_URL=https://Example.com

# Memos API 的基础地址 (必须带 http:// 或 https://，结尾不要加斜杠 /)
# 例如: https://memos.mintimate.cn 或 http://127.0.0.1:5230
# 注意：PUBLIC_ 前缀是 Astro 客户端变量规范，浏览器端动态加载时必须使用此名称
PUBLIC_MEMOS_API_BASE=http://127.0.0.1:5230
